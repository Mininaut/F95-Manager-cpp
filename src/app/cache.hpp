#pragma once
// Port target: src/app/cache.rs
// Purpose: Caching layer for metadata and images during downloads.

#include <string>

namespace app {
namespace cache {

// Placeholder API mirroring Rust responsibilities:
// bool init(const std::string& cache_dir);
// bool put(const std::string& key, const std::string& data_path);
// std::string get(const std::string& key);
// bool remove(const std::string& key);

} // namespace cache
} // namespace app
