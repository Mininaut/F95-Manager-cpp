#pragma once
// Port target: src/views/cards/items/card.rs
// Purpose: Model/logic for a single card in the cards view.

#include <string>
#include <vector>

namespace views {
namespace cards {
namespace items {

struct Card {
    // std::string title;
    // std::string subtitle;
    // std::vector<std::string> tags;
    // std::string cover_path;
    // bool installed = false;
};

// Placeholder API:
// void render(const Card& c);

} // namespace items
} // namespace cards
} // namespace views
